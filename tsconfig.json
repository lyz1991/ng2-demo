{
  "compilerOptions": {
    "target": "es5", //编译后脚本标准使用es5
    "module": "commonjs", //模块编译后的表现形式是commonjs的语法规范
    "moduleResolution": "node", //使用node的方式管理模块
    "sourceMap": false, //使用sourcemap, 这样断点后能直接看到源码
    "emitDecoratorMetadata": true,//使ng2支持Decorator装饰器
    "experimentalDecorators": true,//使ng2支持Decorator装饰器
    "removeComments": false, //编译后不移除注释
    "noImplicitAny": true,//禁止使用隐式any, 意思就是一定要标识类型, 即使它是any
    "suppressImplicitAnyIndexErrors": true //因为标识了any是禁止的所以会报错, 但是我们不想显示这种错误, 所以禁止他显示any的错误
  }
}
